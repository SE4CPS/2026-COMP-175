# Module 3: File Systems & Storage

Slide content for this module is still being published. The
[`catalog-scripts/`](catalog-scripts/) folder is real, though -- it's the
"catalog-scripts" repo Lab 2 has students `git clone` into
`/srv/cataloging` on their Ubuntu server VM, then search with `find`/`grep`.
