# Module 3: File Systems & Storage

Slide content for this module is still being published. Two folders here
are real, though:

| Folder | What it's for |
|---|---|
| [`catalog-scripts/`](catalog-scripts/) | The "catalog-scripts" repo Lab 2 has students `git clone` into `/srv/cataloging`, then search with `find`/`grep`. |
| [`dotfiles/`](dotfiles/) | A sample `.bashrc` admin welcome banner, built from the commands (`id`, `groups`, `mount`, `df`) this module teaches. |
